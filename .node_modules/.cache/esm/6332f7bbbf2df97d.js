let Joi,CustomErrorHandler,RefreshToken,User,bcrypt,JwtService,REFRESH_SECRET;_bf2‍.x([["default",()=>_bf2‍.o]]);_bf2‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_bf2‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_bf2‍.w("../../models",[["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}],["User",["User"],function(v){User=v}]]);_bf2‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_bf2‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);_bf2‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);






const registerController = {
   async register(req, res, next){

        //validate the request
            const registerSchema = Joi.object({
                name:Joi.string().min(3).max(30).required(),
                email:Joi.string().email().required(),
                password:Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$')).required(),
                repeat_password:Joi.ref('password')
            });
            _bf2‍.g.console.log(req.body)
            const { error }= registerSchema.validate(req.body);

            if (error){
                return next(error);
            }

            //Check if user in the database already
            try{
                const exist = await User.exists({email:req.body.email});
                if(exist){
                    return next(CustomErrorHandler.alreadyExist('This Email is already Taken'));
                }
            }catch(err){
                return next(err);
            }

            //hash password

            const{name, email,password}=req.body;
            const hashPassword = await bcrypt.hash(req.body.password, 10);

        
        //prepare model
                const user = new User({
                    name:name,
                    email:email,
                    password:hashPassword


                })
        // store in database
        let access_token;
        let refresh_token;
                try{
                    const result = await user.save();
                    _bf2‍.g.console.log(result);
                  // generate jwt token
                    access_token = JwtService.sign({_id:result._id, role:result.role});
                    refresh_token = JwtService.sign({_id:result._id, role:result.role}, '1y',REFRESH_SECRET);
                
                    // database whitelist fro refresh token

                    await RefreshToken.create({token:refresh_token});


                }catch(err){
                    return next(err);
                }

     

        // send response


        res.json({access_token,refresh_token});
    }
}


_bf2‍.d(registerController);